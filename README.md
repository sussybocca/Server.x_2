# Server.x_2
this is the better version of server.x instead of servers being deleted and not actually being stored anywhere and not being seen this will have an real supabase database which I love by the way
